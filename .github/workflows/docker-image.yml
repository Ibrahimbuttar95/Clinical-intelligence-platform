name: Backend Docker Build

on:
push:
branches:
- main
workflow_dispatch:

jobs:
docker:
runs-on: ubuntu-latest

steps:
  - uses: actions/checkout@v4

  - name: Build Backend Docker Image
    run: |
      docker build \
        -f backend/Dockerfile \
        -t clinical-intelligence-backend \
        backend
